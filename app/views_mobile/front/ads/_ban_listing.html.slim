- index = ad_service.id_for(:ban_listing)

div id="ban-listing#{index}"
  javascript:
    Adf.banner.showScroll("ban-listing#{index}", {
      'p1': 'bvknm',
      'p2': 'emil',
      'pct': 'a',
      'puid49': "listing#{index}"
    });
